# =============================================================================
# Python 3.11 / 3.12 / 3.13 compatible.
#
# Versions are expressed as floors (>=), not exact pins, so the resolver
# can pick wheels that exist for whichever Python the host platform uses
# (Railway/Streamlit Cloud default to 3.13 as of 2026).
#
# If you need reproducibility, generate a lockfile with:
#     pip install pip-tools
#     pip-compile requirements.txt -o requirements.lock
# =============================================================================

# ---- Core ----
streamlit>=1.40
pandas>=2.2.3
numpy>=2.1
python-dotenv>=1.0
pyyaml>=6.0

# ---- Database ----
SQLAlchemy>=2.0
# psycopg2-binary>=2.9     # uncomment for Postgres

# ---- HTTP / scraping ----
requests>=2.32
beautifulsoup4>=4.12
lxml>=5.3
lxml_html_clean>=0.4       # required by lxml ≥5 + any html-cleaning code
feedparser>=6.0
trafilatura>=2.0           # replaces abandoned newspaper3k for article extraction
tenacity>=9.0
# playwright>=1.48          # uncomment for JS-rendered sites

# ---- NLP (optional) ----
langdetect>=1.0
# openai>=1.40              # uncomment for LLM summarization
# deep-translator>=1.11     # uncomment for ZH→EN

# ---- Charts ----
plotly>=5.24
# pydeck is bundled with streamlit; no need to pin separately

# ---- Scheduling ----
APScheduler>=3.10
schedule>=1.2

# ---- Export ----
openpyxl>=3.1
reportlab>=4.2

# ---- Logging ----
loguru>=0.7
